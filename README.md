# act — Public Release Repo

This repository distributes `act` plugin files and pre-built
binaries for the [Claude Code](https://claude.ai/code) plugin
marketplace. Source code lives in a private repository.

Install:

```
claude plugin marketplace add act101-ai/act101
claude plugin install act@act-marketplace
```

See the plugin README in `plugin/README.md` for details.
